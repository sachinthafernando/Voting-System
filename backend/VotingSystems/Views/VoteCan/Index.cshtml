@model IEnumerable<VotingSystems.Models.VoteCan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Candidate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Candidate.CandidateID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VoteCanID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VoteCanID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VoteCanID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
